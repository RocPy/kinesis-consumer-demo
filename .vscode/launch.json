{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Remote Docker Attach",
            "type": "python",
            "request": "attach",
            "port": 5678,
            "host": "localhost",
            "redirectOutput": true,
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}/kinesis_consumer",
                    "remoteRoot": "/app/kinesis_consumer"
                }
            ]
        }
    ]
}